# reading-timestamp (타임스탬프)

Stamps the date on book-reading proof photos for a reading group. Upload a photo → the app extracts the EXIF datetime (or accepts manual input) → composites a 1080×1080 image with a timestamp overlay at the bottom. Download the result.

Stack: Vanilla HTML/JS, Canvas API, custom EXIF parser, PWA.
